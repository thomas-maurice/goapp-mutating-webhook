apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mutating-webhook.fullname" . }}-config
  labels:
    {{- include "mutating-webhook.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- with .Values.config }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
